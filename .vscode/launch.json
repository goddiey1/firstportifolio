{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "file": "${workspaceFolder}/index1.html"
        }
    ]
}